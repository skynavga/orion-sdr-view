# A COFDM link below the FEC cliff.
#
#   orion-sdr-view --headless --script scripts/cofdm-degraded.txt --dump run.jsonl
#
# 5 dB is below the cliff at every bandwidth fraction: no frame gets through, so
# the BER rungs report `null` rather than a rate.  It used to need a `--config`
# beside it; an untimed `set` is that config file, in the recipe.
#
# What to look for in the dump: `cber`, `iber` and `mer_db` all carry
#   {"v": null, "prov": "unavailable"}
# rather than a zero.  Nothing decoded, so there was nothing to measure — which
# is a different statement from "the error rate was zero", and the reason a CSV
# column would have been the wrong choice here.  Note the provenance too: the
# reading is absent because no provider could supply it, not because a receiver
# looked and found nothing.

set run.duration 20
set cofdm.cn_db  5.0

0.00 source COFDM
