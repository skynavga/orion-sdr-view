# Walk all six sources, pausing on each.
#
# A smoke test, and a tour of the dump: `source` records mark each switch and
# carry the new sample rate (48 kHz for the five narrowband sources, 1.92 MHz for
# COFDM), `info` and `text` records show what each one decodes, and `gap` records
# mark the silences between bursts.
#
# Each stop is named rather than counted.  A file that walks the whole list is
# exactly the one a new or reordered source would break, and it would break
# quietly: `key I` still parses and still runs, just onto a different source.

set run.duration 40

0.00  source CW
6.00  source AM DSB
12.00 source PSK31
18.00 source FT8
24.00 source COFDM
30.00 source Test Tone   # one more press: COFDM is last, so `I` wraps to the first
